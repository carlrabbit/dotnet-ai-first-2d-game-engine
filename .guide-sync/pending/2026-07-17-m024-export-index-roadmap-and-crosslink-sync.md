# Guide Sync Hint — M024 Export Index, Roadmap, and Cross-Link Synchronization

## Status

Pending.

After M024 acceptance:

- index the export and game-host specs, export artifact family, ADR-0032, ADR-0033, commands, wrappers, and M024;
- update the roadmap to mark the first standalone Linux export complete;
- distinguish export from SDK, packaging, installer, and release publication;
- document linux-x64 as the only tested export target;
- link M024 performance-report results without treating them as permanent cross-machine budgets;
- update active capability summaries that still stop before M024.

Delete when active project truth consistently describes the implemented narrow export.
