# Guide Sync Hint — Milestone 016 Index and Cross-Link Synchronization

## Status

Pending.

## Purpose

After M016 stabilizes, synchronize indexes and cross-links for input maps/sequences, action types, pointer state, input frames, semantic replay, artifacts, wrappers, ADR-0019, and milestone status.

## Completion criteria

Delete only when M016 and ADR-0019 are indexed; input roots and commands are discoverable; semantic action/pointer/tick semantics and replay compatibility are documented; headless synthetic validation is distinguished from optional physical adapters; physical mouse/controller support is not overstated; and remapping, multiplayer, rollback, text input, save/load, animation, and audio remain deferred.
