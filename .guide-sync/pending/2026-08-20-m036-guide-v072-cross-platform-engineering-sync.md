# Deferred synchronization hint — M036 guide v0.7.2 and cross-platform engineering

## Classification

```text
guide-system metadata only
ordinary implementation agents: must not read
source milestone: M036
```

## Topics for a later guide synchronization pass

Assess whether external project guidance should reflect these generalized lessons:

- guide v0.7.2 baseline-model executability at the `ready` boundary;
- v0.7.1 completion-audit terminal outcomes;
- one platform-neutral engineering semantic host with multiple thin native launcher families;
- supporting Windows development without implying Windows distribution;
- launcher inventory based on current operational value rather than historical milestone retention;
- deleting dead historical shell wrappers when Git already preserves history;
- keeping platform provenance outside semantic fingerprints unless platform is a semantic input;
- cross-host validation where separate platform reports are bound to one repository fingerprint;
- Git as the synchronization boundary for parallel cross-platform development;
- machine-local raw asset homes with platform-native defaults and a portable override.

## Completion criteria

Resolve only in a dedicated documentation-synchronization workflow against the then-current external guide repository.

Do not copy guide documents into this target repository.

M036 implementation does not depend on this file.
