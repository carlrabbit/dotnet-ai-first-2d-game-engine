# Guide Sync Hint — M028 Shared Asset Library and Discovery

## Status

Pending.

After M028 completion:

- add shared asset home, source registry, discovery profile, reusable annotation, campaign, and batch terms to active indexes;
- document the non-runtime and non-source-controlled role of the shared asset home;
- index new product CLI commands and review-pack artifacts;
- record image/audio discovery baseline and cleanup/annotation policy;
- remove text implying M011 static curation is the complete workflow;
- preserve project truth only.

Delete when active documentation reflects accepted M028 behavior.
