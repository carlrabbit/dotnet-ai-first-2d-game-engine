# Guide Sync Hint — v0.5.0 Index and Validation Synchronization

## Status

Pending.

After the migration implementation stabilizes, synchronize active project indexes and cross-links for:

- guide-system version 0.5.0 traceability;
- constrained execution modes;
- resumable suites and shard interfaces;
- validation plans and receipt artifacts;
- engineering command host;
- review commands and `.review/` state;
- platform support;
- ADR-0028, ADR-0029, and M022.

Review milestone validation sections that still instruct agents to run broad no-argument aggregate commands. Replace them with the project-local resumable execution reference where applicable, without duplicating the full contract.

Delete this hint only when active indexes, command docs, milestone routing, and artifact docs are consistent.
