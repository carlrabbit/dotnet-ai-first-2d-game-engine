# Deferred synchronization hint — M037 product shell, UI, saves, settings, and input

## Classification

```text
guide-system metadata only
ordinary implementation agents: must not read
source milestone: M037
```

## Generalizable topics

A later documentation-synchronization task may assess guidance for small engine-owned retained UI toolkits without data binding; separate player/diagnostics compositions; user settings versus immutable world configuration; save-as-player-unit with internal world provenance; editable default save naming; wall-clock autosave requests at valid save boundaries; deterministic configurable retention; safe display preview/rollback/safe mode; explicitly registered bindable actions and context routing; cross-platform product-shell validation with one semantic suite and platform graphics evidence; milestone-scoped UX review; and v0.7.2 completion audits for review-gated product milestones.

## Completion criteria

Resolve only in a dedicated synchronization workflow against the then-current external guide repository. Do not copy guide documents into this target repository. M037 implementation does not depend on this file.
