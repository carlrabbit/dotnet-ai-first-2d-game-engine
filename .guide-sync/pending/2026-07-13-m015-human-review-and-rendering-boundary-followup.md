# Guide Sync Hint — Milestone 015 Human Review and Rendering Boundary Follow-Up

## Status

Pending.

## Review questions

- Can rendering mutate or advance runtime?
- Do raylib types leak outside the adapter?
- Does the headless CLI work without native graphics dependencies?
- Are entity/static ownership and visual references distinct?
- Are PNG/asset/region bindings inspectable?
- Are cache inputs and invalidation credible?
- Is ordering deterministic and tree occlusion understandable?
- Do pause/step/reset use runtime APIs only?
- Do live and artifact modes share one projector?
- Are screenshots explicit review evidence?
- Are native resources cleaned up on all paths?
- Can animation, input, or another backend extend the design without changing runtime authority?

Delete when findings are incorporated, converted into focused follow-up work, or explicitly rejected.
