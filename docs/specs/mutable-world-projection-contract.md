# Mutable World Projection Contract

## Authority

Authoritative for projecting mutable runtime world-object state into collision, spatial participation, interaction, animation, sound, and rendering.

Authored map structure is never rewritten. Runtime state changes explicitly invalidate affected spatial, interaction, and render projections.

Post-load reconstruction restores authoritative door/switch component and collision state before resumed movement or interaction.
